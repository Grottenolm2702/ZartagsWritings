// prisma/schema.prisma

generator client {
  provider = "prisma-client"
  enginetype = "client"
  output   = "../src/generated"
}

datasource db {
  provider = "sqlite"
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String?
  password  String
}
